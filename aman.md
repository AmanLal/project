#include <stdio.h>

int main(void) {
	int n;
	printf("enter the number");
	scanf("%d",&n);
	if (n>0)
	printf("no is positive");
	if(n==0)
	printf("no is zero");
	else
	printf("no is negative");
	return 0;
}
